apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "todo-chatbots.fullname" . }}-test-connection"
  labels:
    {{- include "todo-chatbots.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "todo-chatbots.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
