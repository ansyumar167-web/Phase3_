apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-chatbots.fullname" . }}
  labels:
    {{- include "todo-chatbots.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todo-chatbots.selectorLabels" . | nindent 4 }}
